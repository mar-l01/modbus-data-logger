[Unit]
Description=Start Modbus data logger application as service#
Requires=network.target
After=network.target

[Service]
User=root
Type=simple
ExecStart=/builds/build_native/release/modbus_data_logger
ExecStop=script/executing/a/kill -INT

[Install]
WantedBy=multi-user.target
